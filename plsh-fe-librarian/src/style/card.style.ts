import { color } from "@/helpers/resources";
import { SxProps, Theme } from "@mui/material";

export const informationCardStyle: SxProps<Theme> = {
  color: color.LIGHT_TEXT,
  borderRadius: "10px",
  padding: "21px 15px",
  height: 149,
};