import React, {JSX} from "react";
import App from "@/app/(private)/(in-dash-board)/policy/app";

function page(): JSX.Element {
    return (
        <>
            <App/>
        </>
    );
}

export default (page);

