import React from "react";
import Overview from "@/app/(private)/(in-dash-board)/resources/books/[id]/(read-only)/(page)/index";

async function BookDetailPage() {
    return (
        <Overview/>
    );
}

export default BookDetailPage;
