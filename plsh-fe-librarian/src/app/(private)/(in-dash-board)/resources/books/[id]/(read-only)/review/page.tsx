import React, {JSX} from "react";
import Review from "@/app/(private)/(in-dash-board)/resources/books/[id]/(read-only)/review/index";

function page(): JSX.Element {
    return (
        <Review/>
    );
}

export default page;

